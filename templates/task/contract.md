# <TASK-ID> Task Contract

Risk: Low | Normal | Critical
Contract version:
Baseline SHA:

> This file describes the task contract itself. Once frozen, `contract.md` is immutable for that contract version. Mutable task phase/status and review-target SHAs belong in `evidence.md`.

## Goal

<!-- 1-3 lines: what must be true when the task is complete? -->

## User-visible / operator-visible behavior

<!-- Describe behavior, not implementation detail. -->

## Existing facts

<!-- Record verified facts from code/config/schema/runtime. Cite exact sources. Distinguish fact from inference. -->

## Guarantees / Invariants

<!-- Number these. Reviewers should cite them. -->

## State model / transitions

<!--
Scale this section by Risk and materiality.
- Low: a short description of the relevant states/transitions is enough when state is local and low-risk.
- Normal: enumerate the material transitions and undefined cases.
- Critical/material state machines: use a formal transition table including persistence, side effects, concurrency/intermediate changes, and evidence mapping.
Do not implement while a material state case is undefined.
-->

## Acceptance criteria

## Evidence mapping

| AC / invariant | Evidence | Pass condition |
|---|---|---|

## Non-scope / Non-guarantees

<!-- Explicitly state guarantees intentionally not provided. -->

## Allowed files

## Forbidden changes

## Deployment / rollback

<!-- Project-specific release gates and rollback conditions. -->

## Undefined decisions

<!-- Decisions that ORCH/user must resolve before implementation. -->

If implementation encounters a material undefined case, stop and return:

```text
SPEC_UNDEFINED
- question:
- why needed:
- affected behavior:
- affected files:
- current safe stopping point:
```

## Freeze rule

Freeze by committing the completed contract and recording that commit as the `Frozen contract SHA` in `evidence.md`.
After freeze, do not edit this file for the same contract version. If a material contract change is required, increment `Contract version`, edit the contract, re-review as required, and freeze a new commit.
